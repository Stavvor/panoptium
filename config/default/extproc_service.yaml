apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: panoptium
    app.kubernetes.io/managed-by: kustomize
  name: controller-manager-extproc
  namespace: system
spec:
  ports:
  - name: grpc
    port: 9001
    protocol: TCP
    targetPort: 9001
  selector:
    control-plane: controller-manager
